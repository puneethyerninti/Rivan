{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!assets/|favicon.ico|icons/|manifest|robots.txt|sw.js).*)",
      "destination": "/index.html"
    }
  ]
}
